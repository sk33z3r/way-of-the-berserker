package art.celestro.berserkermod;

import net.minecraft.item.ToolMaterial;

class FlintHatchet extends HatchetItem {
    public FlintHatchet(ToolMaterial material, float attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
