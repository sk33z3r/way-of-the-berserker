package art.celestro.berserkermod;

import net.minecraft.item.ToolMaterial;

class FlintDagger extends DaggerItem {
    public FlintDagger(ToolMaterial material, float attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
